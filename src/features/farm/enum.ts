export enum Chef {
  MASTERCHEF = 'MasterChef',
  MASTERCHEF_V2 = 'MasterChef V2',
  MINICHEF = 'MiniChef',
}

export enum Rewarder {
  SIMPLE,
  COMPLEX,
  ALCX,
}

export enum PairType {
  SWAP,
  KASHI,
}
