const Main = ({ children }) => (
    <main className="flex flex-col items-center justify-start flex-grow w-full h-full px-4 py-4 sm:py-8 md:py-12">
        {children}
    </main>
)

export default Main
